public class BinaryConverter{
    public static void main(String[] args){
        BinaryConverter binaryConverter = new BinaryConverter();
        String binaryString = "0100100001101001";
        Sttring text=converter.binaryToText(binaryInput);\
        Sysstem.out.println("Binary: "+binaryInput);
        System.out.println("Text: "+text);

    }
    public String binaryToText9String binary){
        String result = "";

        for(int i = 0; i<binary.length();i+=8){

            int end = Math.min(i+8,binary.length());

            String block = binary.sunstring(i,end);

            int charCode=binarytoDecimal(block);
            result+=(char)charCode;
        }

        resutn result;

    }

    pulic int binaryToDecimal(String binaryString){
        int decimal = 0;
        int power =0;

        for(int i =binaryString.length()-1;i>=0;i--){
            char bit = binaryString.charAt(i);
            int value = bit -"0";
            decimal +=value *(int)Math.pow(2,power);
            power++
        }
        return decimal;

    }
}